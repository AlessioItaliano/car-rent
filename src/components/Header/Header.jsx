import * as s from './Header.styled';

const Header = () => {
  return (
    <s.HeaderContainer>
      <s.InfoContainer>
        <p>About us</p>
        <p>Contacts</p>
      </s.InfoContainer>
    </s.HeaderContainer>
  );
};

export default Header;
